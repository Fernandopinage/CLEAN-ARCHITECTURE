import {expect, jest, test} from '@jest/globals';

describe('Validate the of mail', () => {

    test('Mail is string ?', () => {    

        const parm = {mail: 'LuizfernandoLuck@hotmail.com'}
        expect(true).toBe(true);
    });
  });